<ion-view view-title="Plan Trip">
<ion-nav-buttons side="secondary">
<button class="button button-icon icon ion-ios-search-strong" ng-click="showFilterBar()"></button>
</ion-nav-buttons>
<ion-content>
<div id="directions-map"></div>
<div class="input-div">
        <div style="margin-bottom: 20px">
                <b>Origin</b>
                <input id="origin-input" class="user-input location-typeahead" type="text" placeholder="Enter an origin location">
        </div>
        <div style="margin-bottom: 20px">
                <b>Destination</b>
                <input id="destination-input" class="user-input location-typeahead" type="text" placeholder="Enter a destination location">
        </div>
        <div style="margin-bottom: 80px">
                <label class="item item-input item-select">
                        <div class="input-label">
                                &nbsp;
                        </div>
                        <select ng-model="time.type" id="time-trip-choice">
                                <option selected value="departure">Time of Departure</option>
                                <option value="arrival">Time of Arrival</option>
                        </select> 
                </label>
                <input type="datetime-local" ng-model="time.datetime" id="trip-time" class="user-input">
                <button ng-click="timeToNow()" id="time-now-button" class="user-input">Set Time to Now</button>
        </div>
        <div>
                <button ng-click="route()" id="route-button" class="user-input">Route</button>
                <button ng-click="saveTrip()" id="save-button" class="user-input">Save Trip</button>
        </div>
</div>
<div style="margin: 30px" ng-if="route.origin != null">
        <b>Origin:</b> {{route.origin}}
        <br>
        <b>Destination:</b> {{route.destination}}
        <br>
        <b>Departure Time:</b> {{route.departure_time}}
        <br>
        <b>Arrival Time:</b> {{route.arrival_time}}
</div>
<ion-list>
<ion-item ng-repeat="step in route.steps" href={{route.step_links[$index]}}>
<p>{{step}}</p>
</ion-item>
</ion-list>
</ion-content>
</ion-view>
